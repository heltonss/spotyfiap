import React from "react";
import Loader from "react-loader-spinner";

const LoaderWave = () => {
  return <Loader type="Audio" color="#91f70e" height={100} width={100} />;
};

export default LoaderWave;
